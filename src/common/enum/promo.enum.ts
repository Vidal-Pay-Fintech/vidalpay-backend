export enum PromoStatus {
  ACTIVE = 'active',
  EXPIRED = 'expired',
  DEACTIVATED = 'deactivated',
}

export enum PromoRedeemStatus {
  SUCCESS = 'success',
  FAILED = 'failed',
}

export enum ReferralRedeemStatus {
  PAID = 'paid',
  PENDING = 'pending',
}
